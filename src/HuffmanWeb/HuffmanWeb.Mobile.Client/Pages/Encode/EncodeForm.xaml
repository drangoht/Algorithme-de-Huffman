<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:commands="clr-namespace:HuffmanWeb.Mobile.Client.Commands"
             x:Class="HuffmanWeb.Mobile.Client.Pages.Encode.EncodeForm"
             
             Title="EncodeForm">
    <ContentPage.BindingContext>
        <commands:EncodeCommandViewModel />
    </ContentPage.BindingContext>
    <VerticalStackLayout VerticalOptions="Fill">
        <Label 
            Text="Saisissez le texte à encoder"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Border 
                    Padding="4,0"
                    HeightRequest="300" >
            <ScrollView HeightRequest="300"
                            HorizontalScrollBarVisibility="Always"
                            VerticalScrollBarVisibility="Always" WidthRequest="380" IsEnabled="True" ZIndex="2000">
                <Editor x:Name="textToEncode" Text="{Binding TextToEncode}"
            AutoSize="TextChanges" />
            </ScrollView>
        </Border>
        <Button Margin="0,10" Padding="10,0" Text="Encoder" HorizontalOptions="Center" Command="{Binding CallEncodeApiCommand}" />
        <Label Margin="10,0" x:Name="encodeBinayResponse" Text="{Binding Response.EncodedBinaryString}" />
    </VerticalStackLayout>
</ContentPage>